# Tasks — Phase 8: Final Polish and Portfolio Readiness

## Overview

Implementation tasks for Phase 8. All tasks are documentation, tooling, and presentation — no new Lambda logic, DynamoDB tables, API endpoints, or Terraform resources are introduced. Every task references exact file paths. Tasks are ordered so that foundational content (README, diagrams) comes before dependent content (walkthrough, demo guide).

---

## Task List

- [x] 1. README Overhaul
  - [x] 1.1 Delete all existing content from `README.md` and replace it with the new structure defined in the design doc: badges row, H1 headline, "What is Radius?" paragraph, embedded Mermaid pipeline diagram, Key Features bullet list, Tech Stack table, Quick Start numbered steps, Test Results code block, Project Walkthrough link, and Contributing paragraph
  - [x] 1.2 Write the "What is Radius?" paragraph (~80 words) explaining blast radius as a concept, how Radius monitors CloudTrail events to detect suspicious IAM behavior, and that it produces explainable Blast Radius Scores with automated remediation — accessible to a non-specialist recruiter
  - [x] 1.3 Add the shields.io badge row to `README.md` with badges for Python 3.11, Terraform, AWS Lambda, DynamoDB, React, and a static "tests passing" badge
  - [x] 1.4 Add the Tech Stack table to `README.md` listing all 10 technologies (Lambda, DynamoDB, EventBridge, API Gateway, SNS, CloudTrail, Terraform, React, Hypothesis, moto) with their roles
  - [x] 1.5 Add the Quick Start section to `README.md` with the five numbered commands: clone, venv, pip install, run-tests.sh, simulate-attack.py
  - [x] 1.6 Add a static Test Results code block to `README.md` showing representative `scripts/run-tests.sh` output (suite names, test counts, coverage percentages)
  - [x] 1.7 Verify `README.md` contains no references to Phase 2 scope, `IMPLEMENTATION_SUMMARY.md`, or any stale content from the previous README

- [x] 2. Architecture Diagrams
  - [x] 2.1 Create `docs/architecture/pipeline-overview.md` with a 2–4 sentence prose introduction and a `flowchart TD` Mermaid diagram showing the full event pipeline: CloudTrail → EventBridge → Event_Normalizer → Detection_Engine → Incident_Processor → Remediation_Engine, with Identity_Collector, Score_Engine, SNS topics, all DynamoDB tables, API Gateway, API_Handler, and React Dashboard annotated
  - [x] 2.2 Create `docs/architecture/scoring-pipeline.md` with a prose introduction and a `flowchart TD` Mermaid diagram showing Score_Engine trigger sources (EventBridge schedule, Event_Normalizer direct invoke), ScoringContext.build() with its four DynamoDB queries, all 8 scoring rules, the sum-and-cap step, ScoreResult, and the Blast_Radius_Score table write
  - [x] 2.3 Create `docs/architecture/remediation-branch.md` with a prose introduction and a `flowchart TD` Mermaid diagram showing the remediation pipeline: Incident_Processor async invoke → Remediation_Engine → load_config → safety controls (with suppression path) → match_rules (with no-match path) → collect actions → Risk Mode branch (monitor/alert/enforce paths) → audit log writes → SNS publish
  - [x] 2.4 Create `docs/architecture/api-layer.md` with a prose introduction and a `flowchart TD` Mermaid diagram showing React Dashboard → API Gateway → API_Handler Lambda → five endpoint groups (Identity, Incident, Score, Event, Remediation) → their respective DynamoDB tables
  - [x] 2.5 Update `docs/architecture.md` to add a "Diagrams" section after the existing "Design Principles" section, with links to all four new diagram files: `pipeline-overview.md`, `scoring-pipeline.md`, `remediation-branch.md`, `api-layer.md`
  - [x] 2.6 Copy the Mermaid `flowchart TD` block from `docs/architecture/pipeline-overview.md` into `README.md` (task 1.1 placeholder) so the pipeline diagram renders directly on the GitHub repository landing page

- [x] 3. Demo Guide
  - [x] 3.1 Create `docs/demo/demo-guide.md` with an Overview section explaining what the demo shows (full privilege escalation attack through the Radius pipeline) and why it matters for demonstrating the platform
  - [x] 3.2 Add a Prerequisites section to `docs/demo/demo-guide.md` listing: Python 3.11+, `pip install -r backend/requirements-dev.txt`, and a note that no AWS credentials are needed in mock mode
  - [x] 3.3 Add an "Attack Scenario" section to `docs/demo/demo-guide.md` with a narrative of the four-step attack (CreateUser → AttachUserPolicy → CreatePolicyVersion → StopLogging) and a table mapping each step to the CloudTrail event it generates and the detection rule it triggers
  - [x] 3.4 Add a "Running the Demo" section to `docs/demo/demo-guide.md` with exact shell commands for each phase: `python scripts/simulate-attack.py --mode mock`, `python scripts/simulate-attack.py --mode mock --verbose`, and `python scripts/simulate-attack.py --mode mock --phase 4`
  - [x] 3.5 Add a "What to Narrate" section to `docs/demo/demo-guide.md` with bullet points for each of the five phases that the presenter can use to explain what is happening to an interviewer
  - [x] 3.6 Add an "Expected Output" section to `docs/demo/demo-guide.md` with an annotated text block showing the full `simulate-attack.py` output for a successful mock run, with inline comments explaining each section
  - [x] 3.7 Add a "Cleanup" section to `docs/demo/demo-guide.md` explaining that mock mode leaves no persistent state (moto is in-process) and that live mode cleanup requires deleting the seeded DynamoDB records
  - [x] 3.8 Update `docs/demo/demo-scenario.md` to add a note at the top cross-referencing `demo-guide.md` and `scripts/simulate-attack.py` as the automated version of the scenario

- [x] 4. simulate-attack.py Script
  - [x] 4.1 Create `scripts/simulate-attack.py` with a `#!/usr/bin/env python3` shebang, a module docstring describing the script's purpose and usage, and `argparse`-based CLI with flags: `--mode {mock,live}` (default: `mock`), `--identity ARN` (default: `arn:aws:iam::123456789012:user/attacker`), `--verbose` (flag), `--phase {1,2,3,4,5}` (optional int), `--timeout SECONDS` (default: 30)
  - [x] 4.2 Implement `setup_mock_aws()` in `scripts/simulate-attack.py` that activates moto's `mock_aws()` context manager and creates all required DynamoDB tables (Identity_Profile, Blast_Radius_Score, Incident, Event_Summary, Remediation_Config, Remediation_Audit_Log) with correct key schemas and GSIs, and seeds Remediation_Config with a default `monitor` mode record
  - [x] 4.3 Implement `phase1_seed_identity(identity_arn, verbose)` in `scripts/simulate-attack.py` that writes an Identity_Profile record to DynamoDB (or moto) for the attacker identity with fields: `identity_arn`, `identity_type=IAMUser`, `account_id`, `first_seen`, `last_activity_timestamp`, `event_count=0`
  - [x] 4.4 Implement `phase2_inject_events(identity_arn, verbose)` in `scripts/simulate-attack.py` that writes four Event_Summary records to DynamoDB representing the attack sequence: `iam:CreateUser`, `iam:AttachUserPolicy`, `iam:CreatePolicyVersion`, `cloudtrail:StopLogging` — each with a realistic timestamp, `identity_arn`, `event_id` (UUID), and `event_parameters`
  - [x] 4.5 Implement `phase3_poll_incident(identity_arn, timeout_s, verbose)` in `scripts/simulate-attack.py` that queries the Incident table's `IdentityIndex` GSI for incidents with `identity_arn` matching the attacker, polling every 2 seconds up to `timeout_s`, returning the first incident found or raising `TimeoutError`; in mock mode, write a synthetic incident record directly after a 2-second delay to simulate pipeline processing
  - [x] 4.6 Implement `phase4_display_score(identity_arn, verbose)` in `scripts/simulate-attack.py` that queries the Blast_Radius_Score table for the attacker identity and prints: score value, severity level, contributing factors list; in mock mode, write a synthetic score record (score=85, severity=Critical) if none exists
  - [x] 4.7 Implement `phase5_show_audit_log(identity_arn, verbose)` in `scripts/simulate-attack.py` that queries the Remediation_Audit_Log table's `IdentityTimeIndex` GSI for the attacker identity and prints the most recent 10 entries formatted as a table; in mock mode, write two synthetic audit entries (suppressed, monitor_mode) if none exist
  - [x] 4.8 Implement `main()` in `scripts/simulate-attack.py` that orchestrates phase execution: if `--phase N` is given, run only that phase; otherwise run all phases in order 1–5, stopping on first failure; collect `PhaseResult` objects and call `print_phase_summary()` at the end; exit with code 0 on all pass, 1 on any failure
  - [x] 4.9 Implement `print_phase_summary(results)` in `scripts/simulate-attack.py` that prints the formatted summary table (phase number, name, status with ✓/✗ prefix, duration) and a final "All phases passed" or "N phase(s) failed" line

- [ ] 5. run-tests.sh Script
  - [ ] 5.1 Create `scripts/run-tests.sh` with `#!/usr/bin/env bash`, `set -euo pipefail`, a comment block describing the script and its `--fast` flag, and argument parsing for `--fast`
  - [ ] 5.2 Implement the `run_suite` function in `scripts/run-tests.sh` that accepts a suite name and pytest command, records start time, runs the command capturing output, records end time, parses pytest output for passed/failed counts, and stores results in parallel arrays for the summary table
  - [ ] 5.3 Add the three suite invocations to `scripts/run-tests.sh`: Unit Tests (`pytest backend/tests/ --ignore=backend/tests/integration --ignore=backend/tests/test_*_properties.py --cov=backend --cov-report=term-missing -q`), Integration Tests (`pytest backend/tests/integration/ --cov=backend --cov-append -q`), and Property-Based Tests (`pytest backend/tests/test_*_properties.py --cov=backend --cov-append -q`) — the third suite is skipped when `--fast` is set
  - [ ] 5.4 Implement `print_summary_table` in `scripts/run-tests.sh` that prints the formatted table with columns: Suite, Tests, Passed, Failed, Coverage, Duration — using the data collected by `run_suite` — followed by a TOTAL row and a final pass/fail message
  - [ ] 5.5 Add the exit code logic to `scripts/run-tests.sh`: accumulate a `TOTAL_FAILURES` counter from all suites; exit 0 if `TOTAL_FAILURES -eq 0`, exit 1 otherwise
  - [ ]* 5.6 Add a `--coverage-html` optional flag to `scripts/run-tests.sh` that appends `--cov-report=html:htmlcov` to the pytest command and prints the path to the HTML report after the summary table

- [ ] 6. Documentation Gap-Filling
  - [ ] 6.1 Add a "Remediation" section to `docs/api-reference.md` after the existing "Incidents" section, documenting all 6 remediation endpoints with full request/response schemas: `GET /remediation/config`, `PUT /remediation/config/mode`, `GET /remediation/rules`, `POST /remediation/rules`, `DELETE /remediation/rules/{rule_id}`, `GET /remediation/audit` (with `incident_id`, `identity_arn`, `limit` query params)
  - [ ] 6.2 Add a "Phase 7 Resources" section to `docs/deployment.md` listing the new Lambda (`{env}-remediation-engine`), two new DynamoDB tables (`{env}-remediation-config`, `{env}-remediation-audit-log`), new SNS topic (`{env}-radius-remediation`), new env vars on `incident_processor` (`REMEDIATION_LAMBDA_ARN`) and `api_handler` (`REMEDIATION_CONFIG_TABLE`, `REMEDIATION_AUDIT_TABLE`), and the IAM permissions added to the `remediation_engine` role
  - [ ] 6.3 Add a "Running Tests" section to `docs/developer-guide.md` with instructions for: installing test dependencies (`pip install -r backend/requirements-dev.txt`), running all tests (`bash scripts/run-tests.sh`), running fast mode (`bash scripts/run-tests.sh --fast`), running a single test file (`pytest backend/tests/test_remediation_engine.py -v`), and interpreting coverage output
  - [ ] 6.4 Add a log group entry for `/aws/lambda/{env}-remediation-engine` to `docs/monitoring.md` with the structured log fields it emits (`function_name`, `correlation_id`, `incident_id`, `identity_arn`, `risk_mode`, `actions_executed`, `actions_suppressed`)
  - [ ] 6.5 Add a "Correlation ID Propagation" section to `docs/monitoring.md` explaining how `correlation_id` flows from Event_Normalizer through Detection_Engine, Incident_Processor, and Remediation_Engine via the invocation payload, and how to use it in CloudWatch Logs Insights
  - [ ] 6.6 Add three example CloudWatch Logs Insights queries to `docs/monitoring.md`: (a) find all log entries for a given `correlation_id`, (b) find all incidents created in the last hour, (c) find all remediation actions executed for a given `identity_arn`
  - [ ] 6.7 Add full schema entries for `Remediation_Config` and `Remediation_Audit_Log` to `docs/database-schema.md`, including: key schema, all GSIs with projection types, TTL configuration, and a complete example record for each table
  - [ ] 6.8 Verify that every documentation file longer than 100 lines has a table of contents; add a ToC to any file that is missing one (`docs/api-reference.md`, `docs/deployment.md`, `docs/developer-guide.md`, `docs/monitoring.md`, `docs/database-schema.md`, `docs/remediation.md`)

- [ ] 7. Repository Hygiene
  - [ ] 7.1 Delete `IMPLEMENTATION_SUMMARY.md` from the repository root
  - [ ] 7.2 Update `.gitignore` to add the following entries if not already present: `__pycache__/`, `*.pyc`, `.pytest_cache/`, `.hypothesis/`, `*.egg-info/`, `dist/`, `build/`, `.env`, `.env.*`, `*.tfstate`, `*.tfstate.backup`, `.terraform/`, `node_modules/`, `frontend/build/`, `coverage.xml`, `.coverage`, `htmlcov/`
  - [ ] 7.3 Create `.python-version` at the repository root containing the single line `3.11`
  - [ ] 7.4 Audit every Python file in `backend/functions/` and add a one-sentence module-level docstring to any file that is missing one; files to check: `event_normalizer/handler.py`, `event_normalizer/normalizer.py`, `detection_engine/handler.py`, `detection_engine/engine.py`, `detection_engine/context.py`, `incident_processor/handler.py`, `incident_processor/processor.py`, `identity_collector/handler.py`, `identity_collector/collector.py`, `api_handler/handler.py`, `api_handler/handlers.py`, `remediation_engine/handler.py`, `remediation_engine/engine.py`, `remediation_engine/audit.py`, `remediation_engine/config.py`
  - [ ] 7.5 Verify that `scripts/build-lambdas.sh`, `scripts/deploy-infra.sh`, and `scripts/verify-deployment.sh` each have a `#!/usr/bin/env bash` shebang and a comment block at the top describing what the script does and its usage; add missing shebangs and comment blocks
  - [ ] 7.6 Verify that `scripts/inject-events.py` and `scripts/seed-dev-data.py` each have a `#!/usr/bin/env python3` shebang and a module docstring; add missing shebangs and docstrings

- [ ] 8. Project Walkthrough Document
  - [ ] 8.1 Create `docs/walkthrough.md` with a top-level H1 heading "Radius — Project Walkthrough" and a one-paragraph introduction explaining that this document is an interview-ready narrative of every design decision in the project
  - [ ] 8.2 Write the "Why I Built Radius" section in `docs/walkthrough.md` (~150 words) covering: the blast radius mental model for IAM risk, the gap in open-source tooling for explainable identity scoring, and the decision to build a complete end-to-end platform rather than a single-function tool
  - [ ] 8.3 Write the "Architecture Decisions" section in `docs/walkthrough.md` covering four decisions with alternatives and rationale: (a) serverless Lambda + DynamoDB vs containerized services, (b) event-driven async invokes vs synchronous pipeline, (c) DynamoDB vs RDS, (d) EventBridge vs Kinesis for CloudTrail routing
  - [ ] 8.4 Write the "Detection Engine Design" section in `docs/walkthrough.md` covering: the two rule types (single-event vs context-aware), why DetectionContext pre-fetches all data before rules run, why deduplication lives in Incident_Processor rather than Detection_Engine, and a link to `docs/detection-rules.md`
  - [ ] 8.5 Write the "Scoring Model Design" section in `docs/walkthrough.md` covering: why rule-based scoring was chosen over ML, how contributing factors make every score explainable, the score cap at 100 and why it prevents correlated-indicator inflation, and a link to `docs/scoring-model.md`
  - [ ] 8.6 Write the "Remediation Design" section in `docs/walkthrough.md` covering: the three risk modes and why monitor is the safe default, the four safety controls and why they prevent runaway automation, the append-only audit log and its compliance value, and a link to `docs/remediation.md`
  - [ ] 8.7 Write the "Testing Strategy" section in `docs/walkthrough.md` covering: unit tests (fast, isolated, mock boto3), integration tests (moto for realistic AWS behavior), and property-based tests (Hypothesis invariants: severity ordering, round-trip serialization, monitor mode suppression) — with a link to `scripts/run-tests.sh`
  - [ ] 8.8 Write the "What I Would Do Differently" section in `docs/walkthrough.md` with three items: (a) add Kinesis for high-volume environments, (b) add a feedback loop for false-positive rate tracking, (c) replace static confidence values with Bayesian updating
  - [ ] 8.9 Write the "Common Interview Questions" section in `docs/walkthrough.md` with 8 Q&A pairs covering: scalability to 1,000 accounts, cost model at 10M events/day, preventing false positives from triggering remediation, adding a new detection rule, ensuring audit log tamper-resistance, the blast radius of Radius itself being compromised, adding real-time streaming, and what would change about the scoring model
  - [ ] 8.10 Verify `docs/walkthrough.md` is between 1,500 and 3,000 words and add a table of contents at the top linking to each section
